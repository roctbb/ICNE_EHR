@model WebApplication2.Models.newResearch
@using WebApplication2.Models
@using (Html.BeginForm("Create", "Researches", FormMethod.Post, new { id = "ResearchesCreate" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.visitID)
    @Html.HiddenFor(model => model.num)
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="row">
            <div class="col-md-4">
                <strong>
                    @Html.DropDownListFor(model => model.research.type.ID, new SelectList(Model.eventTypes, "ID", "name"), new { @class = "form-control" })
                </strong>
            </div>
            <div class="col-md-6">
                @Html.EditorFor(model => model.research.description, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.research.description, "", new { @class = "text-danger" })
            </div>
            <div class="col-md-2">
                <a onclick="PostCreateForm('Researches', @Model.num)" class="btn btn-primary btn-sm"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span></a>

            </div>
        </div>
        <div class="row" style="margin-top:10px">
            <div class="col-md-4">

            </div>
            <div class="col-md-6  alert alert-info">
                <h4><span class="glyphicon glyphicon-comment" aria-hidden="true"></span> @Html.DisplayNameFor(model => model.research.comments):</h4>
                @Html.EditorFor(model => model.research.comments, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.research.comments, "", new { @class = "text-danger" })
            </div>
        </div>
</div>
}