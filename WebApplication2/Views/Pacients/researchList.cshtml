
@model WebApplication2.Models.Pacient
@using WebApplication2.Models

<div class="bs-callout bs-callout-primary">
    <h5>Добавить данные</h5>
    <p>&nbsp;</p>
    @Html.Action("pacientCreateByDate", "Researches", new { id = Model.ID })
</div>
<div class="bs-callout bs-callout-success">
    <h5>Исследования</h5>
    <p>&nbsp;</p>
    <div class="tabContent">
        @foreach (var visit in Model.visits)
        {
            foreach (var anamnes in visit.researches)
            {
                @Html.Partial("~/Views/Researches/pacientDetails.cshtml", anamnes)
            }
        }
        </div>

    </div>
